# Ce dossier reçoit des fichiers envoyés par les utilisateurs : il ne doit
# jamais pouvoir exécuter de PHP, même si un fichier malveillant y était
# déposé sous un nom trompeur.
<FilesMatch "\.(php|phtml|php3|php4|php5|php7|phar)$">
    Require all denied
</FilesMatch>
